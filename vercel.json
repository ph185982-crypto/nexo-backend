{
  "framework": "nextjs",
  "buildCommand": "cd vendedoria && npm ci && npm run build",
  "outputDirectory": "vendedoria/.next/standalone",
  "installCommand": "echo 'skip'",
  "crons": [
    { "path": "/api/campaign/worker", "schedule": "* * * * *" }
  ]
}
